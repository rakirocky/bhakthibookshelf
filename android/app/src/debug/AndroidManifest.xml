<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only overlay. Points the app at a local dev server for on-device
  testing of the offline-downloads feature:
   - usesCleartextTraffic + a network security config that also trusts a
     local dev CA (res/raw/dev_ca.pem), so https://<lan-ip>:3010 loads
     with a trusted cert and the WebView is a secure context (WebCrypto).
  Never merged into release builds.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:replace="android:usesCleartextTraffic,android:networkSecurityConfig" />
</manifest>
